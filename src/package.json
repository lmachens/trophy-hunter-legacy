{
  "private": true,
  "name": "trophy-hunter",
  "scripts": {
    "api": "yarn --cwd \"api\" dev",
    "www": "yarn --cwd \"www\" dev"
  },
  "workspaces": [
    "api",
    "www"
  ],
  "devDependencies": {
    "typescript": "^3.2.2"
  }
}
