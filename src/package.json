{
  "private": true,
  "name": "trophy-hunter",
  "scripts": {
    "api": "yarn --cwd \"api\" dev",
    "build-overwolf": "yarn --cwd \"overwolf\" build",
    "www": "yarn --cwd \"www\" dev"    
  },
  "workspaces": [
    "api",
    "overwolf",
    "www"
  ],
  "devDependencies": {
    "typescript": "^3.2.2"
  }
}
